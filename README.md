# POSIX Threads example

## Task

integrate f(x) on [a;b] using POSIX Threads

## Requirements

* pthread library

## Build

* change dir to `build`
* run `cmake ..`
* run `make`

## Run

* run `pthread_example` from `build` dir
